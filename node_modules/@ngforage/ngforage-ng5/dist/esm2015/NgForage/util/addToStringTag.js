/** @internal */
export let addToStringTag;
if (typeof Symbol !== 'undefined') {
    addToStringTag = (target, tag) => {
        target.prototype[Symbol['toStringTag']] = tag;
    };
}
else {
    // tslint:disable-next-line:no-empty
    addToStringTag = () => {
    };
}
